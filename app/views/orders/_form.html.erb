<%= simple_form_for @order, html: { id: :orderform } do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <div align="center">
    <div class="container">
        <%= f.input :order_profile_id, :input_html => {:id => "selected_profile_id", :value => "", :class => "hidden"} %>
      <!-- big button logo begins -->
      <div class="form-actions">
        <%= image_submit_tag("button-logo.svg", class: "", id: "submitorder") do %>
          <script src="https://checkout.stripe.com/checkout.js"></script>
          = heidden_field_tag 'stripeToken'
          = hiddne_field_tag 'stripeEmail'
          <script type="text/javascript">
          // var handler = StripeCheckout.configure({
          //   key: '<%= ENV["PUBLISHABLE_KEY"] %>',
          //   token: function(token, args) {
          //     document.getElementById("stripeToken").value = token.id;
          //     document.getElementById("stripeEmail").value = token.email;
          //   }
          // });

          document.getElementById('submitorder').addEventListener('click', function(e){
            handler.open({
              name: 'CoffeeBreak',
              description: 'coffee delivery',
              amount: '100',
            });
            e.preventDefault();
          });
          </script>
          <% end %>
      </div>
      <!-- big button logo ends -->
      <div class="dropdown">
        <button class="btn btn-default dropdown-toggle" type="button" id="menu1" data-toggle="dropdown">
          Order Profiles<span class="caret"></span>
        </button>
        <ul class="dropdown-menu" role="menu" aria-labelledby="menu1">
          <% @order_profiles.each do |o| %>
          <li class="profile_options">
            <%= o['name'] %>
          </li>
          <li style="visibility:hidden"><%= o['id'] %>
          <% end %>
        </ul>
      </div>
    </div>
    <p id="notice"><%= notice %></p>
  </div>
  </div>
<% end %>
